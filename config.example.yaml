YAML configuration file for the U-Blox GPS reader
---
device:
    path: /dev/ttyACM0
    type: u-blox7
    baudRate: 9600
    navRate: 2

plugins:
    mqtt:
        # disabled: true
        host: localhost
        # host: host.docker.internal
        port: 1883
        topic: gps
        username: 
        password: 
        retain: false
        qos: 0

    console:
        # disabled: true
        message: [GLL, TXT]
